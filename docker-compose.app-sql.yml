version: "3"

services:

  todo:
    build:
      context: ./src
      dockerfile: Dockerfile.sql
    ports:
      - "8080:80"
    depends_on:
      - sql
    restart: on-failure
    environment:
      DB_USER: root
      DB_PASSWORD: secret

  sql:
    image: postgres
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: secret

  sql-ui:
    image: adminer
    ports:
      - "8081:8080"
